# Copy to .env.local and fill values locally
# Never commit real secrets

# MongoDB
MONGODB_URI=
MONGODB_DB=

# NextAuth
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Initial Admin (one-time init)
# Used by /api/init to create the first admin when no users exist
INIT_ADMIN_EMAIL=
INIT_ADMIN_PASSWORD=
INIT_ADMIN_NAME=
